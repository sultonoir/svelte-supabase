<script>
	import { fade } from 'svelte/transition';
	import { activeSection, activeSectionStore } from '../stores/activeLink';

	const links = [
		{
			name: 'Home',
			hash: '#home'
		},
		{
			name: 'About',
			hash: '#about'
		},
		{
			name: 'Projects',
			hash: '#projects'
		},
		{
			name: 'Skills',
			hash: '#skills'
		},
		{
			name: 'Experience',
			hash: '#experience'
		},
		{
			name: 'Contact',
			hash: '#contact'
		}
	];

	activeSection;
</script>

<!-- Bagian HTML untuk navbar -->
<header class="z-[999] relative">
	<div
		class="fixed top-0 left-1/2 h-[4.5rem] w-full rounded-none border border-white border-opacity-40 bg-white bg-opacity-80 shadow-lg shadow-black/[0.03] backdrop-blur-[0.5rem] sm:top-6 sm:h-[3.25rem] sm:w-[36rem] sm:rounded-full dark:bg-gray-950 dark:border-black/40 dark:bg-opacity-75 -translate-x-1/2"
	/>
	<nav
		class="flex fixed top-[0.15rem] left-1/2 h-12 -translate-x-1/2 py-2 sm:top-[1.7rem] sm:h-[initial] sm:py-0"
	>
		<ul
			class="flex w-[22rem] flex-wrap items-center justify-center gap-y-1 text-[0.9rem] font-medium text-gray-500 sm:w-[initial] sm:flex-nowrap sm:gap-5"
		>
			{#each links as link}
				<li class="h-3/4 flex items-center justify-center relative">
					<a
						on:click={() => {
							activeSectionStore.set(link.name, Date.now());
						}}
						class={` flex w-full items-center justify-center px-3 py-3 hover:text-gray-950 transition dark:text-gray-500 dark:hover:text-gray-300`}
						href={link.hash}>{link.name}</a
					>
					{#if $activeSection === link.name}
						<span
							transition:fade
							class="bg-gray-100 rounded-full absolute inset-0 -z-10 dark:bg-gray-800 right-0 left-0 transition-transform:scale transition-opacity ease-in-out duration-300"
						/>
					{/if}
				</li>
			{/each}
		</ul>
	</nav>
</header>
